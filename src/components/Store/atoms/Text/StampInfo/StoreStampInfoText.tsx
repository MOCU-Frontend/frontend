import React from 'react';
import styles from './StoreStampInfoText.module.css';

interface Props {}

const StoreStampInfoText: React.FC<Props> = ({}: Props) => {
  return <div>StoreStampInfoText</div>;
};

export default StoreStampInfoText;
